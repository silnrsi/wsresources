Legacy font encoding:  IPAKiel phonetic fonts

Compiled and uncompiled conversion tables are included here, which map IPAKiel data to Unicode and vice versa.

One character at x4d is using the phonetic equivalent. The character is not in Unicode. It looks like an "m" with a left stem ascender.

This mapping file has only been minimally tested. Feedback is requested.

wstech@sil.org